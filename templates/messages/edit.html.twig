{% extends 'base.html.twig' %}

{% block title %}Edit Messages{% endblock %}

{% block body %}
    <h1>Edit Messages</h1>

    {{ form_start(form) }}
    <label for="tickets_title">Comment</label>
    <div class="form-group">
        {{ form_widget(form.content, { 'attr': {'class': 'form-control'} }) }}
    </div>
    <div class="d-flex justify-content-start">
    <button class="btn btn-success edit-button">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}
        {{ include('messages/_delete_form.html.twig') }}
    </div>

    <a href="{{ path('tickets_index') }}">back to list</a>

{% endblock %}
